// Typed bridge between the React frontend and the Rust backend.
// All invoke()/listen() calls go through this module — never call
// @tauri-apps/api directly from components (see CLAUDE.md §4).
// Populated in later tasks.

export {};
